services:
  mongo:
    image: mongo:7
    container_name: ledger-sync-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ledger
      MONGO_INITDB_ROOT_PASSWORD: ledger
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: